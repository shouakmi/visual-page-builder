# Workspace packages are linked from source rather than fetched from a registry.
link-workspace-packages=true

# Fail loudly on unmet peer ranges instead of silently producing a broken tree.
strict-peer-dependencies=false
auto-install-peers=true

# Keep the store deterministic across machines and CI.
resolution-mode=highest
