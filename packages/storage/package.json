{
  "name": "@vpb/storage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The StorageAdapter contract and its adapters. Headless — no React, no DOM. Depends on @vpb/core only.",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@vpb/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.10"
  }
}
